# RSTPM Strict Execution Rules
# Project: 基于AIS数据的实时船舶轨迹预测模型（RSTPM）
# Single source of truth: 方案3.md
# Goal: 禁止漂移、禁止漏文件、禁止伪完成、禁止 silently 改方案

你正在开发/维护的是一个严格按《方案3》实现的 RSTPM 项目。
你的首要职责不是“自由发挥优化”，而是“严格对齐方案、完整落地框架、保证文件闭合、保证上下游契约一致、保证后续AI可持续接手”。
除非用户明确批准，否则不得擅自改算法、改流程、改关键参数含义、改产物格式、改输入输出契约。

==================================================
一、最高优先级原则
==================================================

1. 单一真源原则
- 《方案3.md》是业务流程、核心步骤、关键算法、输入输出链路的最高真源。
- README、代码注释、临时想法、历史实现都不能凌驾于方案3之上。
- 一旦现有代码与方案3冲突，优先指出冲突，不得悄悄合理化。

2. 禁止静默漂移
- 不允许把方案中的步骤“等价替换”为其它算法，除非用户明确允许。
- 不允许因为实现麻烦就删减、合并、跳过任何关键步骤。
- 不允许把“建议值”伪装成“已严格验证的定值”。
- 不允许把“未完成”说成“已完成”。
- 不允许把“占位实现”说成“真实实现”。

3. 完整性优先于表面可运行
- 不能只让脚本勉强运行，而忽略文件缺失、模块缺失、测试缺失、产物缺失、配置缺失、接口缺失。
- 任何新增导入、路径、模块、脚本、资源、配置，都必须真实存在并闭合。
- 文档中提到的目录结构、脚本入口、配置文件、产物文件，不得与实际仓库不一致。

4. 工程加固允许，但不得改变语义
- 允许为大数据量场景引入增量处理、分块处理、checkpoint、缓存、parquet 中间产物、日志、断点续跑、schema 校验、类型约束、测试夹具。
- 但这些工程优化不能改变方案3的算法语义、步骤顺序、核心输入输出关系。

==================================================
二、开始任何任务前的强制流程
==================================================

在写代码、改代码、删代码、审查代码之前，必须先执行以下顺序：

1. 先读取并理解：
- 方案3.md
- 项目目录树
- README
- config / schemas / scripts / tests / data_processing / clustering / model / online / utils 等核心目录
- 与本次任务相关的具体文件

2. 先建立“任务边界”
你必须先明确：
- 本次任务属于方案3的哪一步或哪几步
- 会影响哪些上游输入
- 会影响哪些下游产物
- 需要修改哪些文件
- 需要新增哪些文件
- 需要补哪些测试
- 需要更新哪些文档/配置/脚本

3. 先做“方案映射”
在脑中或回复中明确：
- 方案要求是什么
- 当前代码已有的东西是什么
- 缺口是什么
- 本次改动只解决哪些缺口
- 哪些问题本次不解决

4. 禁止先写后想
- 不得未阅读相关代码就盲改
- 不得未确认现有接口就新建一套平行接口
- 不得未确认已有配置就硬编码新参数
- 不得未确认现有文件结构就写错误 import

==================================================
三、硬性方案约束（不可擅改）
==================================================

以下内容默认视为“硬性业务约束”，没有用户明确授权不得改语义：

------------------------------
步骤1：原始AIS加载
------------------------------
- 必须围绕 MMSI / Timestamp / LON / LAT / SOG / COG 组织主流程。
- 必须按 MMSI 分组、按时间升序排序。
- 允许保留其它静态字段，但不得让其干扰主流程。
- 如原始字段名不同，必须做明确映射，不得偷偷假设字段名已一致。

------------------------------
步骤2：异常值过滤
------------------------------
必须保留并实现以下五类过滤语义：
1) 地理范围过滤
2) 航速合理性过滤
3) 航向合理性过滤
4) 相邻点跳跃距离过滤（基于时间差与隐含速度）
5) 重复点过滤
- 不得只实现其中一部分却声称“已完成步骤2”。
- 不得把基于相邻点的跳跃过滤错误改成简单的全局阈值过滤。
- 不得忽略时间差为正这一前提判断。

------------------------------
步骤3：停泊点检测与删除
------------------------------
- 必须保留“窗口扩展 + 空间阈值 + 低速阈值 + 时间跨度阈值”的组合逻辑。
- 不得简化为单纯 SOG < 某阈值即删除。
- 不得忽略“短暂停留保留、长时间停泊删除”的区分。
- 停泊段删除后要考虑对后续步骤4轨迹切分的影响。

------------------------------
步骤4：轨迹切分
------------------------------
- 必须基于同一 MMSI 的时间间隔阈值进行航次切分。
- 必须生成独立 TrajID 或等价唯一轨迹标识。
- 必须保留最小轨迹长度过滤逻辑。
- 不得把短轨迹过滤提前到错误阶段导致语义改变。

------------------------------
步骤5：插值与等时间重采样
------------------------------
- 必须把不等时间间隔轨迹转换为等时间间隔序列。
- 必须对 LON / LAT / SOG 做线性插值。
- 对 COG 必须考虑角度环绕问题，不得把 350° -> 10° 当普通线性差值。
- 重采样时间序列必须可追溯、可复现。
- 工程上允许分块/增量处理，但算法语义不得改变。

------------------------------
步骤6：CDDTW 聚类
------------------------------
步骤6是核心，不得随意替换为其它聚类套路。必须保留下列结构性语义：
6A. 提取每条轨迹起点和终点
6B. 对起点/终点做 Ball-Tree 优化的 DBSCAN 空间聚类
6C. 在初始类别内基于改进 DTW 做轨迹相似度细化
6D. 处理方向相反但路线相同的类别合并（含 reverse 比较）
6E. 输出类别、特征轨迹、聚类质量信息

进一步硬性要求：
- 不得把 Ball-Tree+DBSCAN 偷换成 KMeans / 纯层次聚类 / 纯 HDBSCAN 等而不声明。
- 不得把改进 DTW 偷换成普通欧式距离而声称“同等实现”。
- 不得省略“特征轨迹”的定义与保存。
- 不得省略“方向翻转比较/合并”的逻辑。
- 不得只输出类别标签而不输出在线阶段所需的特征轨迹资产。

------------------------------
步骤7：按簇归一化
------------------------------
- 必须按聚类类别分别计算 LON / LAT / SOG / COG 的归一化参数。
- 不得使用一个全局统一 scaler 替代按簇归一化，除非用户明确批准改方案。
- 必须持久化保存归一化参数，在线阶段必须复用相同参数。
- 必须处理 max=min 的零除边界。

------------------------------
步骤8：样本构造
------------------------------
- 必须按轨迹序列构造滑动窗口样本。
- 输入应为 T x 4，标签应为 1 x 4 或等价单步输出格式。
- 训练/验证/测试划分必须按“轨迹”为单位，不得按样本对随机拆分造成数据泄漏。
- 时间步长 T 必须有配置来源，不得在各处硬编码冲突值。

------------------------------
步骤9：每簇独立 LSTM 模型训练
------------------------------
- 必须保留“每个聚类类别对应一个模型”的基本策略，不得擅自改成全局单模型。
- 模型主干必须是 LSTM，不得偷偷换成 GRU / Transformer / MLP 而仍声称符合方案。
- 必须保存模型文件、模型配置、归一化参数引用、特征轨迹引用、训练日志等关键资产。
- 必须保留验证集与早停/最佳模型选择逻辑。
- 批量大小、学习率、dropout、epoch 等参数允许配置化，但不能让配置来源混乱。

------------------------------
步骤10：在线实时预处理
------------------------------
- 必须维护按 MMSI 的实时缓冲区。
- 必须在在线阶段复用步骤2的关键合法性检查语义。
- 必须处理长时间中断重置缓冲区。
- 必须支持把不规则到达的点插值/补齐为等间隔输入序列。
- 在尚未确定类别前，缓冲区策略必须能兼容不同类别的 T。

------------------------------
步骤11：在线类别匹配
------------------------------
- 必须基于当前短轨迹片段与各类别特征轨迹做匹配。
- 必须考虑在特征轨迹上滑动窗口，而不是只比较整条特征轨迹。
- 匹配核心语义应是方案中的改进 DTW 轨迹相似比较，不得无说明替换。
- 必须支持“未知类别”或“不匹配”处理分支。
- 多分叉河段逻辑不能被删掉；如暂未落地，必须明确标注为未完成而不是假装完成。

------------------------------
步骤12：在线推理与多步预测
------------------------------
- 必须用步骤7保存的同簇归一化参数对输入归一化、对输出反归一化。
- 必须支持单步预测与递推多步预测。
- 多步预测必须使用滑动窗口递推逻辑，不能伪装成一次性输出未来全部点而不说明。
- 必须保留预测时长受误差累积影响的工程约束。
- 输出必须包含预测时间与预测状态序列，格式需稳定可复用。

==================================================
四、参数规则
==================================================

1. 参数分层必须明确
所有参数必须分成三类并清楚标识：
- HARD_SPEC: 方案硬约束或必须保留的业务语义
- DEFAULT_RECOMMENDED: 方案给出的默认/推荐值
- TUNABLE: 可通过实验调整，但必须集中配置且有说明

2. 禁止参数漂移
- 不得在多个文件中重复定义相同参数而数值不一致。
- 不得在函数内部散落硬编码魔法数。
- 所有关键参数必须有单一配置来源，并能被日志打印或保存到产物元数据中。

3. 对于方案中已写明的典型参数
例如：
- Vmax
- k
- epsilon_d
- epsilon_v
- T_gap
- L_min
- DeltaT_resample
- DBSCAN R / MinP
- Batch Size
- Epoch
- Dropout
- lr
- T
- H
这些参数应优先从统一配置读取，并在训练/推理资产中可追溯。

==================================================
五、文件与目录完整性规则
==================================================

1. 任何 import 必须可追溯到真实文件
- 不允许出现 README 中有目录、仓库里没有目录
- 不允许出现代码里 import 的模块实际不存在
- 不允许出现脚本入口引用不存在的配置或类

2. 文档-代码-目录三者必须一致
- README 中列出的目录树必须与真实目录树一致
- CLI 示例命令必须与真实脚本路径一致
- 配置字段名必须与代码读取字段一致
- 中间产物路径与文件格式必须与代码保存/加载逻辑一致

3. 新增文件时的强制要求
新增任何文件时必须明确：
- 它的职责是什么
- 上游依赖是什么
- 下游谁会用到它
- 是否需要测试
- 是否需要加入 README / 配置 / scripts / __init__ 暴露

4. 删除文件时的强制要求
删除任何文件前必须确认：
- 没有脚本/模块再引用它
- 没有测试依赖它
- README 和说明文档已同步更新
- 不会造成“目录还在说明中、实际文件已删”的不一致

==================================================
六、防伪完成规则
==================================================

1. 未完成逻辑必须显式暴露
- 不允许留 pass 然后说已实现
- 不允许写 TODO 然后说已支持
- 若必须占位，优先使用清晰异常或明确的 NotImplementedError，并标注原因、预期输入输出、后续位置

2. 不允许“只写接口、不写契约”
- 类、函数、脚本、schema、产物格式必须相互对齐
- 输入 shape、输出 shape、字段名、单位、时间格式必须明确

3. 不允许“只改一半”
例如：
- 新增了配置字段，但未在脚本接入
- 新增了产物保存，但未实现加载
- 新增了模型训练，但在线推理还沿用旧资产格式
- 改了数据结构，但 tests / schemas / readers 未同步

==================================================
七、代码风格与实现约束
==================================================

1. 优先可维护、可审计、可接力
- 代码要模块清晰、函数职责单一、命名稳定
- 关键算法步骤要有简短注释解释“为什么这样做”
- 不要用炫技写法掩盖逻辑

2. 类型、schema、数据契约
- 关键中间产物尽量用 dataclass / TypedDict / pydantic / schema 校验等方式固定结构
- 训练资产、聚类资产、归一化参数、在线输入输出，必须有稳定结构
- 严禁靠“猜 key 名”串联上下游

3. 日志与异常
- 关键路径必须有日志：加载、过滤、切分、重采样、聚类、训练、匹配、推理、保存、加载
- 遇到不满足前提的数据或资产，必须明确报错，而不是 silent fail

4. 对大数据量的工程规则
- 面对全量 AIS 数据，默认优先采用增量/分块/中间落盘/断点续跑策略
- 但不得因为工程压力而篡改算法语义
- 任何工程捷径都必须明确说明“是否与方案语义等价”

==================================================
八、测试与验收门禁
==================================================

每次任务完成前，至少做以下自检：

1. 结构完整性检查
- import 是否全部有效
- 新增文件是否被正确纳入目录与入口
- README / 配置 / scripts / tests 是否同步

2. 方案符合性检查
- 本次改动对应方案哪一步
- 是否保留了该步骤的核心语义
- 是否引入了新的漂移
- 是否影响上下游契约

3. 运行层检查
- 至少保证相关模块可导入
- 关键脚本参数解析正常
- 关键测试/新增测试可以运行
- 关键中间产物能保存也能加载

4. 审计式输出
在回复中必须清楚说明：
- 改了哪些文件
- 新增了哪些文件
- 删除了哪些文件
- 每个文件改动目的是什么
- 对应方案哪一步
- 当前仍未完成的部分是什么
- 风险点是什么
- 跑了哪些测试
- 未验证的内容是什么

==================================================
九、回答格式规则
==================================================

每次完成编码任务后，回复必须尽量包含：

1. 本次任务归属
- 属于方案第几步/哪几个子步骤

2. 修改概览
- 新增文件
- 修改文件
- 删除文件

3. 方案对齐说明
- 本次如何对齐方案
- 哪些地方保持硬一致
- 哪些地方只是工程优化且未改语义

4. 风险与未完成项
- 明确写出未完成内容
- 明确写出需要后续补的测试或资源
- 不得模糊表述成“基本没问题”

==================================================
十、禁止事项清单
==================================================

禁止做以下事情，除非用户明确授权：
- 把 CDDTW 替换为完全不同的聚类框架
- 把 LSTM 替换为其它主模型
- 把按簇归一化改成全局归一化
- 把按轨迹划分数据集改成按样本随机划分
- 删除在线类别匹配步骤
- 删除多分叉河段逻辑
- 省略特征轨迹保存
- 省略归一化参数保存
- 省略模型资产元数据
- 省略测试却声称“项目已完整”
- README 树与实际目录不一致
- import 路径失效
- 只修表面报错，不修真实契约问题
- 把猜测当事实写进代码或文档

==================================================
十一、最终目标
==================================================

你的最终目标不是“尽快给出一堆代码”，而是：
- 让项目严格围绕方案3落地
- 让目录、文件、脚本、配置、测试真实闭合
- 让后续AI接手时不需要猜
- 让任何一步都可追溯到方案3中的对应要求
- 最大限度减少缺文件、缺模块、伪完成、参数漂移、接口漂移、步骤漂移